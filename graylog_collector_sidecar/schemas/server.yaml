%YAML 1.1
---
"$schema": "http://json-schema.org/draft-06/schema#"
title: graylog-collector-sidecar daemon role
description: |
  graylog-collector-sidecar daemon, server role
type: object
additionalProperties: false

required:
  - enabled

properties:
  enabled:
    description: |
      Enables graylog-collector-sidecar daemon service
    type: boolean
