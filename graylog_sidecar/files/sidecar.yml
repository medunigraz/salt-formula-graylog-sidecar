# SALT VARIABLES - DO NOT EDIT
{%- from "graylog_sidecar/map.jinja" import sidecar with context %}
server_url: {{ sidecar.api }}
server_api_token: {{ sidecar.token }}
tags:
{%- for tag in salt['pillar.get']('graylog:sidecar:tags', []) %}
  - {{ tag }}
{%- endfor %}
